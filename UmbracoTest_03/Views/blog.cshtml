@inherits Umbraco.Web.Mvc.UmbracoViewPage
@{
	Layout = "WebBase.cshtml";
}

@* the fun starts here *@

	<div id="fh5co-blog-section">
        <div class="container">
            @{Html.RenderAction("RenderTitleControls", "SiteLayout");}

            @{Html.RenderAction("RenderPostList", "Blog");}
        </div>
	</div>